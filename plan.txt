1. Read `src/SignalProcessing/FMCW.hs` and update `chirpZTransform`.
2. Cache the floating-point divisions `b_zoom / k_max` and `(-2) * pi / fs` outside of `calculateBin`.
3. Add a journal entry to `.jules/bolt.md`.
4. Pre-commit tests.
5. Submit PR.
