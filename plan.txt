1. Read `.jules/bolt.md` to see if it exists, create if missing, and append a learning about extracting loop invariant divisions in Haskell's map closures.
2. Modify `src/SignalProcessing/FMCW.hs` using `replace_with_git_merge_diff` to hoist `b_zoom / k_max` and `(-2) * pi / fs` outside of `calculateBin` closure in `chirpZTransform`.
3. Verify file changes by running tests.
4. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
5. Submit PR with the "⚡ Bolt: [performance improvement]" format.
