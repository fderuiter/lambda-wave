## 2026-05-17 - Discoverable Interactive Canvas
**Learning:** When using `<canvas>` for interactive visualizations, `cursor: pointer` is insufficient for discoverability.
**Action:** Always add a descriptive `title` attribute and explicit visual text instructions (e.g. "Click or press Space to resume") directly on the canvas when paused/empty to improve usability.
