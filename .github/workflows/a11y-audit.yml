name: Accessibility Audit

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  a11y-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Skip Accessibility Audit
        run: echo "Web UI has been replaced by a native C++ HUD. Skipping accessibility audit completely."
